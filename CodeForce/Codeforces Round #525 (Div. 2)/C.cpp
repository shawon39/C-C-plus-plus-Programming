#include <bits/stdc++.h>
using namespace std;

#define fast ios_base::sync_with_stdio(0);cin.tie(NULL);cout.tie(NULL)
#define ll long long int

int main()
{
    fast;
    ll n; cin >> n;
    vector<ll>v;
    for(ll i=0; i<n; i++) {
        ll a; cin >> a;
        v.push_back(a);
    }

    for(int i=0; i<n; i++)
    {

    }


    return 0;
}

