#include <stdio.h>

int main() {

    fast; int row,col,k;
    cin >> row >> col >> k;

    for(int i=0; i<row; i++)
    {
        for(int j=0; j<col; j++)
        {
            cin >> ch[i][j];
        }
    }

    for(int i=0; i<row; i++)
    {
        for(int j=0; j<col; j++)
        {
            if(ch[j][i]=='.') ct++;

        }
    }

    return 0;
}

