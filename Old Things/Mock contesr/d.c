#include <stdio.h>
int main()
{
    int t,count,i,num,k;
    long long int y;
    scanf("%d",&t);
    for(k=1; k<=t; k++)
    {
        count=0;
        scanf("%d",&num);
        for(i=1; i<=num; i++)
        {
            scanf("%lld",&y);
            if(y=='1')
        }
        printf("Case %d: %d\n",k,count);
    }
    return 0;
}
