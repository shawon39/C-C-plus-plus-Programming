#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main()
{
    int i,j,T,a,b,c,d,count,m1,m2;
    scanf("%d",&T);
    while(T--)
    {
        count = 0;
        scanf("%d %d %d %d", &a, &b, &c, &d);

        m1 = fabs(a-c);
        m2 = fabs(b-d);

        if(a==c){
            count
        }
    }
}
